from setuptools import setup

setup(
    name="ouvrai",
    packages=["ouvrai"],
    install_requires=[
        "pandas",
        "numpy",
        "openpyxl",
        "seaborn",
        "statsmodels",
        "scipy",
        "IPython",
    ],
)
