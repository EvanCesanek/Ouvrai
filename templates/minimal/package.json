{
  "name": "weblab_minimal",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"vite\" \"firebase emulators:start\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "weblab": "../.."
  }
}
